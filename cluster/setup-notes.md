# Cluster setup notes

- Install `eksctl` and `kubectl`.
- Create the cluster with the provided `eksctl` config.
- Validate the cluster with `kubectl get nodes`.
- Install Kubeflow components or use a fitting training platform.
